{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug current file (inspect)",
      "program": "${file}",
      "runtimeArgs": ["--inspect-brk"],
      "cwd": "${fileDirname}",
      "console": "integratedTerminal"
    }
  ]
}
